{
  "name": "travelpayouts_e2e",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "cypress": "^4.5.0",
    "cypress-failed-log": "^2.7.0",
    "cypress-plugin-retries": "^1.5.2",
    "ls": "^0.2.1",
    "mocha": "^7.1.2",
    "mochawesome": "^6.1.1",
    "mochawesome-merge": "^4.0.3",
    "rimraf": "^3.0.2",
    "yargs": "^15.3.1"
  },
  "scripts": {
    "cypress": "CYPRESS_baseUrl=http://localhost cypress open",
    "cypress:all": "CYPRESS_baseUrl=http://localhost CYPRESS_RETRIES=3 cypress run --config video=false --browser chrome",
    "cypress:reports": "CYPRESS_baseUrl=http://localhost CYPRESS_RETRIES=3 node cypress_runner -b chrome"
  }
}
